= form_for(@diagnosis) do |f|
  - if @diagnosis.errors.any?
    #error_explanation
      %h2
        = pluralize(@diagnosis.errors.count, "error")
        prohibited this diagnosis from being saved:
      %ul
        - @diagnosis.errors.full_messages.each do |msg|
          %li= msg
  .actions
    = f.submit
