%h1 Auswertung der Diagnose
%br/
%h2 So wurde beantwortet:
%table
  %th Fragen Nr.
  %th "Deine" Nr.
  %th Frage 
  %th Richtige Nummer
  %th Richtige Antwort
  - @diagnosed_array.each do |d|
    %tr
      %td.center
        = d[0]
        \.
      %td.center
        = d[1]
        \.
      %td
        = @questions.find(d[0]).content
      %td.green_bold.center
        = @questions.find(d[0]).right.to_s
        \.
      %td.green_bold
        = case @questions.find(d[0]).right.to_s       |
          when "1" then @questions.find(d[0]).answer1 |
          when "2" then @questions.find(d[0]).answer2 |
          when "3" then @questions.find(d[0]).answer3 |
          when "4" then @questions.find(d[0]).answer4 |
          end                                         |
%h2
  Du hast also #{@diagnosed_right_size} richtig.
/ <h2>Array: <haml:loud> @diagnosed_array </haml:loud></h2>
= link_to "Erneut", :action => "index"
%br/
= link_to "Übungsaufgaben drucken", :controller => "latex", :action => "index", :format => :pdf
%br/
%br/
%br/

-#%p 
-#  Raw from form:
-#  = @diagnosed_raw
-#%p 
-#  Array after sorting through:
-#  = @diagnosed_array
-#%p
-#  Right:
-#  = @diagnosed_right
-#%p
-#  Array minus Right:
-#  = @diagnosed_array - @diagnosed_right
-#%p
-#  ex:
-#  - @diagnosed_ex.each do |e|
-#    = e.to_s
-#%p
-#  session diagnosed_ex_s:
-#  = @diagnosed_exs
